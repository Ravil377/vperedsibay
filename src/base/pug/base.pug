include ../../mixins/pug/svg-icon
block variables
    -
        var baseDir = './';

doctype html
html(lang="ru")
    //- Include head
    include ./head
    body(class=pageClass)
        block vreped
        block modal
        script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js')
        script(src=`${baseDir}js/main.js`)
        script(src="https://kit.fontawesome.com/4114794905.js" crossorigin="anonymous")
        block script
